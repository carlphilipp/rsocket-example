spring:
  rsocket:
    server:
      port: 7000
      transport: websocket
  data:
    mongodb:
      host: localhost
      port: 33843

logging:
  level:
    root: INFO
    com.slalom.rsocket.demo: DEBUG
    org.springframework.beans: INFO
